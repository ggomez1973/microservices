spring.application.name: core-whiskyes-sample

spring:
  cloud:
    config:
      uri: http://localhost:44943
  output:
    ansi:
      enabled: ALWAYS

management:
     endpoints.web.exposure.include: '*'
     endpoint:
       health:
         show-details:always